#!/bin/bash

cd ~/arch-install/config/

cp --update=older -R ~/.config/awesome .
cp --update=older -R ~/.emacs.d .
cp --update=older -R ~/.Xresources .
cp --update=older -R ~/.zshrc .
cp --update=older -R ~/.emacs .
